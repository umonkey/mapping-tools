[project]
name = "mapping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gpxpy>=1.6.2",
    "piexif>=1.1.3",
    "pillow>=12.1.0",
]
